# This empty file lives at the project root so pytest adds the root to its
# import path. That lets the tests use "from src.recommender import ..."
# reliably no matter which folder pytest is launched from.
